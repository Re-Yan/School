#include <stdio.h>
#include <stdlib.h>

int main(){
    int stkSize;

    printf("Enter the size of the Stack: ");
    scanf("%d", &stkSize);

    printf("The size of the stack is: %d", stkSize);

    // int *queue = malloc(stkSize * sizeof(int));


    return 0;
}